/**
 * hooks/useAuth.js
 * 
 * Re-export of useAuth from AuthContext for convenience
 * This allows importing from hooks folder consistently
 */

export { useAuth, useRequireAuth, useRequireRole } from '@context/AuthContext'
